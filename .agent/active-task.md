# Active Task

Purpose: Keep the current task state short and current.
Belongs here: The active goal, constraints, and immediate next step.
Does not belong: Long historical logs or completed-task diary entries.

## Current
- Task: Add explanatory headers/comments to code files directory-by-directory and rewrite each directory README around actual behavior and environment assumptions.
- Status: Completed passes for `capture/`, `restore/`, `sys-installs/`, `homefiles/`, `scripts/`, `hooks/`, `systemd/`, `state-templates/`, and `nvim/`.
- Constraints: Read files before editing, keep comments concise and block-oriented, and keep README claims tied to verified behavior rather than guesses.
- Next step: If requested, do a final top-level README pass or move on to a different repo task.
